// Moved to src/storage.ts in Sprint 10 when inspection photos became
// the second consumer — re-exported here so existing imports hold.
export { getStorage, setStorageForTesting } from "../../storage.js";
